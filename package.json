{
  "name": "stripe-demo",
  "version": "1.0.0",
  "description": "This is a stripe payment integration with MERN stack app.",
  "main": "server.js",
  "engines": {
    "node": "16.13.0"
  },
  "scripts": {
    "start": "nodemon backend/server.js",
    "server": "node backend/server.js",
    "client": "npm start --prefix frontend",
    "heroku-postbuild": "NODE_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "Mozahedul Islam",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "stripe": "^8.188.0"
  }
}
